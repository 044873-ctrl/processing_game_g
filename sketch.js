let player,bullets,enemies,particles,stars,score,enemySpawnTimer,bulletCooldown,gameOver
function setup(){createCanvas(400,600);player={x:width/2,y:height-40,r:16,speed:5};bullets=[];enemies=[];particles=[];stars=[];score=0;enemySpawnTimer=0;bulletCooldown=0;gameOver=false;for(let i=0;i<30;i++){stars.push({x:random(0,width),y:random(0,height),s:random(1,3)})}}
function draw(){background(0);for(let i=0;i<stars.length;i++){let st=stars[i];st.y+=st.s;if(st.y>height){st.y=0;st.x=random(0,width)}fill(200);noStroke();ellipse(st.x,st.y,2,2)}if(!gameOver){if(keyIsDown(LEFT_ARROW)){player.x-=player.speed}if(keyIsDown(RIGHT_ARROW)){player.x+=player.speed}player.x=constrain(player.x,player.r,width-player.r);if(bulletCooldown>0){bulletCooldown--}if(keyIsDown(32)&&bulletCooldown===0){bullets.push({x:player.x,y:player.y-player.r,r:4,vy:-8});bulletCooldown=8}enemySpawnTimer++;if(enemySpawnTimer>=60){enemies.push({x:random(12,width-12),y:-12,r:12,vy:2});enemySpawnTimer=0}}for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];b.y+=b.vy;fill(255,255,0);noStroke();ellipse(b.x,b.y,b.r*2,b.r*2);if(b.y<-b.r){bullets.splice(i,1)}}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];if(!gameOver){e.y+=e.vy}fill(255,0,0);noStroke();ellipse(e.x,e.y,e.r*2,e.r*2);let dx=e.x-player.x;let dy=e.y-player.y;let dist2=dx*dx+dy*dy;let pr=player.r+e.r;if(dist2<=pr*pr){gameOver=true}let hit=false;for(let j=bullets.length-1;j>=0;j--){let b=bullets[j];let dx2=e.x-b.x;let dy2=e.y-b.y;let dist22=dx2*dx2+dy2*dy2;let rr=e.r+b.r;if(dist22<=rr*rr){for(let k=0;k<5;k++){let angle=random(0,TWO_PI);let speed=random(1,3);particles.push({x:e.x,y:e.y,r:3,vx:cos(angle)*speed,vy:sin(angle)*speed,life:20})}bullets.splice(j,1);score++;hit=true;break}}if(hit){enemies.splice(i,1)}else if(e.y>height+e.r){enemies.splice(i,1)}}for(let i=particles.length-1;i>=0;i--){let p=particles[i];p.x+=p.vx;p.y+=p.vy;p.life--;fill(255,150,0);noStroke();ellipse(p.x,p.y,p.r*2,p.r*2);if(p.life<=0){particles.splice(i,1)}}fill(255);textSize(16);noStroke();text('Score: '+score,10,20);if(gameOver){fill(255);textSize(32);textAlign(CENTER,CENTER);text('GAME OVER',width/2,height/2)}}
