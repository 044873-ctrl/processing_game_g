let stars=[];let bullets=[];let enemies=[];let particles=[];let player={x:200,y:560,r:16,speed:5};let score=0;let gameOver=false;let shootCooldown=10;let lastShot=-100;function setup(){createCanvas(400,600);for(let i=0;i<30;i++){stars.push({x:random(width),y:random(height),r:random(1,3),s:random(0.5,1.5)});}noStroke();textFont('sans-serif');}function draw(){background(0);for(let i=0;i<stars.length;i++){let s=stars[i];s.y+=s.s;if(s.y>height){s.y=0;s.x=random(width);}fill(255);ellipse(s.x,s.y,s.r*2,s.r*2);}if(!gameOver){if(keyIsDown(LEFT_ARROW)){player.x-=player.speed;}if(keyIsDown(RIGHT_ARROW)){player.x+=player.speed;}player.x=constrain(player.x,player.r,width-player.r);if(keyIsDown(32) && frameCount-lastShot>=shootCooldown){bullets.push({x:player.x,y:player.y-player.r,r:4,speed:8});lastShot=frameCount;}if(frameCount%60===0){enemies.push({x:random(12,width-12),y:-12,r:12,speed:2});}for(let i=bullets.length-1;i>=0;i--){let b=bullets[i];b.y-=b.speed;if(b.y<-b.r){bullets.splice(i,1);}else{fill(255,200,0);ellipse(b.x,b.y,b.r*2,b.r*2);}}for(let i=enemies.length-1;i>=0;i--){let e=enemies[i];e.y+=e.speed;if(e.y>height+e.r){enemies.splice(i,1);continue;}let hitPlayer=false;let dxp=player.x-e.x;let dyp=player.y-e.y;let dp=sqrt(dxp*dxp+dyp*dyp);if(dp<player.r+e.r){gameOver=true;hitPlayer=true;}if(!hitPlayer){for(let j=bullets.length-1;j>=0;j--){let b=bullets[j];let dx=b.x-e.x;let dy=b.y-e.y;let d=sqrt(dx*dx+dy*dy);if(d<e.r+b.r){for(let k=0;k<5;k++){particles.push({x:e.x,y:e.y,vx:random(-2,2),vy:random(-2,2),r:3,life:20});}bullets.splice(j,1);enemies.splice(i,1);score++;break;}}}for(let i=0;i<enemies.length;i++){let e=enemies[i];fill(200,50,50);ellipse(e.x,e.y,e.r*2,e.r*2);}for(let i=particles.length-1;i>=0;i--){let p=particles[i];p.x+=p.vx;p.y+=p.vy;p.life-=1;if(p.life<=0){particles.splice(i,1);}else{let alpha=map(p.life,0,20,0,255);fill(255,150,0,alpha);ellipse(p.x,p.y,p.r*2,p.r*2);}}}if(!gameOver){fill(50,150,255);ellipse(player.x,player.y,player.r*2,player.r*2);}else{fill(120);ellipse(player.x,player.y,player.r*2,player.r*2);textAlign(CENTER);fill(255,0,0);textSize(32);text("GAME OVER",width/2,height/2);textAlign(LEFT);}fill(255);textSize(16);text("Score: "+score,8,18);}
